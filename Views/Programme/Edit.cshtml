@model Programme
@inject IEmployeeRepository _empRepository

@{
    ViewBag.Title = "Edit Programme";
    var _emp = new MockEmployeeRepository();
    ViewBag.employees = _emp.GetAllEmployees().Count();
}
<h5>Update Course</h5>
<form asp-controller="programme" asp-action="edit" method="post" class="mt-3">
    <input type="hidden" asp-for="ProgrammeId" />
    <div class="form-group row">
        <label asp-for="Name" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Name" class="form-control" placeholder="Course Title" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group row">
        <label asp-for="Cost" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Cost" class="form-control" placeholder="Course Code" />
            <span asp-validation-for="Cost" class="text-danger"></span>
        </div>
    </div>

    <div class="form-group row">
        <label asp-for="Duration" class="col-sm-2 col-form-label"></label>
        <div class="col-sm-10">
            <input asp-for="Duration" class="form-control" placeholder="Instructor" />
            <span asp-validation-for="Duration" class="text-danger"></span>
        </div>
    </div>

    <div class="text-danger" asp-validation-summary="All"></div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary form-control">Update</button>
        <a class="btn btn-deafault border" asp-controller="programme" asp-action="index">Cancel</a>
    </div>
</form>